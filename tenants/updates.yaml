apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSet
metadata:
  name: updates
  namespace: flux-system
  annotations:
    fluxcd.controlplane.io/reconcileEvery: "5m"
spec:
  dependsOn:
    - apiVersion: apiextensions.k8s.io/v1
      kind: CustomResourceDefinition
      name: imageupdateautomations.image.toolkit.fluxcd.io
  inputs:
    - namespace: "updates-apps"
      repository: "https://github.com/ssouthcity/homelab-apps.git"
      pushBranch: "image-updates"
    - namespace: "updates-infra"
      repository: "https://github.com/ssouthcity/homelab-infra.git"
      pushBranch: "image-updates"
  resources:
    - apiVersion: v1
      kind: Namespace
      metadata:
        name: << inputs.namespace >>
    - apiVersion: v1
      kind: ConfigMap
      metadata:
        name: flux-runtime-info
        namespace: << inputs.namespace >>
        annotations:
          fluxcd.controlplane.io/copyFrom: "flux-system/flux-runtime-info"
    - apiVersion: v1
      kind: Secret
      metadata:
        name: github-auth
        namespace: << inputs.namespace >>
        annotations:
          fluxcd.controlplane.io/copyFrom: "flux-system/github-auth"
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: flux
        namespace: << inputs.namespace >>
    - apiVersion: rbac.authorization.k8s.io/v1
      kind: RoleBinding
      metadata:
        name: flux
        namespace: << inputs.namespace >>
      roleRef:
        apiGroup: rbac.authorization.k8s.io
        kind: ClusterRole
        name: admin
      subjects:
        - kind: ServiceAccount
          name: flux
          namespace: << inputs.namespace >>
    - apiVersion: source.toolkit.fluxcd.io/v1
      kind: GitRepository
      metadata:
        name: << inputs.namespace >>
        namespace: << inputs.namespace >>
      spec:
        provider: github
        interval: 1h
        ref:
          branch: main
        secretRef:
          name: github-auth
        url: << inputs.repository >>
    - apiVersion: kustomize.toolkit.fluxcd.io/v1
      kind: Kustomization
      metadata:
        name: << inputs.namespace >>
        namespace: << inputs.namespace >>
      spec:
        serviceAccountName: flux
        targetNamespace: << inputs.namespace >>
        interval: 1h
        retryInterval: 5m
        wait: true
        timeout: 5m
        sourceRef:
          kind: GitRepository
          name: << inputs.namespace >>
        path: "./update-policies"
        prune: true
        postBuild:
          substituteFrom:
            - kind: ConfigMap
              name: flux-runtime-info
    - apiVersion: image.toolkit.fluxcd.io/v1
      kind: ImageUpdateAutomation
      metadata:
        name: << inputs.namespace >>
        namespace: << inputs.namespace >>
      spec:
        interval: 1h
        sourceRef:
          kind: GitRepository
          name: << inputs.namespace >>
        git:
          checkout:
            ref:
              branch: main
          commit:
            author:
              email: fluxcdbot@users.noreply.github.com
              name: fluxcdbot
            messageTemplate: |
              chore: fluxcd automated image update

              Files:
              {{ range $filename, $_ := .Changed.FileChanges -}}
              - {{ $filename }}
              {{ end -}}

              Objects:
              {{ range $resource, $changes := .Changed.Objects -}}
              - {{ $resource.Kind }} {{ $resource.Name }}
                Changes:
              {{- range $_, $change := $changes }}
                  - {{ $change.OldValue }} -> {{ $change.NewValue }}
              {{ end -}}
              {{ end -}}
          push:
            branch: << inputs.pushBranch >>
        update:
          path: "./components"
          strategy: Setters
